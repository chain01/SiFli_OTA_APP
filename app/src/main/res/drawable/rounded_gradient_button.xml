<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android" android:shape="rectangle">
    <!-- 设置渐变颜色 -->
    <gradient
        android:startColor="@color/main_blue_1"
        android:endColor="@color/main_blue_2"
        android:angle="0" />
    <!-- 设置圆角半径 -->
    <corners android:radius="10dp" />
    <!-- 设置边框颜色和宽度（可选） -->
    <stroke android:color="@color/main_blue_1" android:width="0dp" />
</shape>